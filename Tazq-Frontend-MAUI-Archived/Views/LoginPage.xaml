<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Tazq_Frontend.ViewModels"
             xmlns:converters="clr-namespace:Tazq_Frontend.Converters"
             xmlns:components="clr-namespace:Tazq_Frontend.Views.Components"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Tazq_Frontend.Views.LoginPage"
             x:Name="LoginPageRoot"
             Shell.NavBarIsVisible="False"
             NavigationPage.HasNavigationBar="False"
             Background="{AppThemeBinding Light={StaticResource AppBackgroundBrushLight}, Dark={StaticResource AppBackgroundBrushDark}}">

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="Transparent" StatusBarStyle="{AppThemeBinding Light=DarkContent, Dark=LightContent}" />
    </ContentPage.Behaviors>

    <Grid>
        <!-- Background Animation Layer -->
        <components:AnimatedBackground />

        <ScrollView>
            <Grid Padding="40,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header Area -->
                <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,60,0,40" HorizontalOptions="Center">
                    <Image Source="{Binding IsLightThemeEnabled, Converter={StaticResource HomePageLogoConverter}}"
                           WidthRequest="140"
                           Aspect="AspectFit" />
                    <Label Text="Giriş yap ve odaklan."
                           FontSize="14"
                           TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                           HorizontalOptions="Center"
                           Opacity="0.8" />
                </VerticalStackLayout>

                <!-- Auth Form -->
                <VerticalStackLayout Grid.Row="1" Spacing="20" VerticalOptions="Center" 
                                     MaxWidthRequest="400" HorizontalOptions="Center">
                    
                    <Border Style="{StaticResource GlassBorder}" Padding="0">
                        <VerticalStackLayout Padding="2">
                            <Entry Placeholder="E-posta"
                                   Text="{Binding Email}"
                                   Keyboard="Email"
                                   Margin="16,4" />
                            <BoxView Style="{StaticResource BoxView}" HeightRequest="1" Margin="16,0" Opacity="0.1" />
                            <Entry Placeholder="Şifre"
                                   Text="{Binding Password}"
                                   IsPassword="True"
                                   Margin="16,4"
                                   Completed="OnPasswordCompleted" />
                        </VerticalStackLayout>
                    </Border>

                    <Label Text="Şifreni mi unuttun?"
                           TextColor="{StaticResource Primary}"
                           FontSize="13"
                           HorizontalOptions="End"
                           Margin="0,0,5,0"
                           IsVisible="{Binding ShowForgotPassword}">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToForgotPasswordCommand}" />
                        </Label.GestureRecognizers>
                    </Label>

                    <Grid>
                        <Button Text="Giriş Yap"
                                Command="{Binding LoginCommand}"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}" />
                        
                        <ActivityIndicator IsRunning="{Binding IsLoading}"
                                           IsVisible="{Binding IsLoading}"
                                           Color="White"
                                           WidthRequest="24"
                                           HeightRequest="24"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
                    </Grid>

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="6">
                        <Label Text="Henüz hesabın yok mu?" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                        <Label Text="Kayıt Ol" TextColor="{StaticResource Primary}" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateToRegisterCommand}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Footer / Social Login -->
                <VerticalStackLayout Grid.Row="2" Spacing="16" Margin="0,40,0,20">
                    <Label Text="veya" 
                           FontSize="12" 
                           TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}" 
                           HorizontalOptions="Center" />

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">
                        <!-- Google -->
                        <Border WidthRequest="56" HeightRequest="56" 
                                StrokeShape="RoundRectangle 28"
                                Padding="14">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference LoginPageRoot}, Path=BindingContext.GoogleLoginCommand}" />
                            </Border.GestureRecognizers>
                            <Image Source="google_logo" />
                        </Border>

                        <!-- Apple -->
                        <Border WidthRequest="56" HeightRequest="56" 
                                StrokeShape="RoundRectangle 28"
                                Padding="14">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference LoginPageRoot}, Path=BindingContext.AppleLoginCommand}" />
                            </Border.GestureRecognizers>
                            <Image Source="apple_logo" />
                        </Border>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Grid>
        </ScrollView>
    </Grid>
</ContentPage>
